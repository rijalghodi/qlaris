import { ShoppingBagIcon } from "lucide-react";

export const Logo = (props: React.SVGProps<SVGSVGElement>) => {
  return <ShoppingBagIcon className="size-6! text-primary" strokeWidth={2.5} {...props} />;
};
